# Bring-Up Status Details

Detailed implementation state for Nokia Lumia 520 / `fame` mainline bring-up.

| Area | Status | Current State | Next Work |
| --- | --- | --- | --- |
| Workspace | Candidate | Top-level submodules and notes are seeded; kernel and U-Boot submodules are active local branches. | Commit once reviewed; update submodule pointers as active branches are pushed. |
| Live BootMgr inventory | Historical | `lp-externals` has safe `NOKV` and `NOKT`; known `TYPE=RM-914`, `CTR=059S083`; live `UEFI` now contains raw APPSBL U-Boot, not Nokia BootMgr. | Restore BootMgr/FlashApp only when fresh Lumia inventory or FFU recovery is explicitly needed. |
| FFU corpus | Working | Stock RM-914 / `059S083` FFU, emergency package, partition images, and PLAT ACPI artifacts are inventoried under `notes/`. | Keep extracted blobs under ignored `extracted/`; record only metadata and decoded facts. |
| ACPI/PCFG | Working | FFU PLAT DSDT/SSDT are decompiled; display PCFG is extracted and names the 480x800 Teisko DSI panel. | Decode GPIO/resource flags before enabling touch, SDCC3 card-detect, panel reset, or regulators. |
| Kernel | Candidate | `linux/` carries minimal MSM8227 GSBI5 UART, USB1/ULPI PHY, TLMM, SDCC1/eMMC pinctrl, MSM8930-style RPM/QFPROM/TSENS, TLMM-owned PSHOLD, and PM8038 L3/L4/L5/L11 regulator wiring; `qcom_defconfig` includes configfs ACM for the test gadget; the latest image boots to initrd, full `ttyMSM0`, RPM-regulated USB/eMMC supplies, eMMC, UDC, and CDC-ACM gadget bind. | Debug remaining non-fatal USB regulator device-link/voltage warnings and bring up secondary CPU support separately. |
| Kernel boot harness | Working | `./build-linux-fastboot.sh` builds `Image.gz`, Android boot-image v2 DTB payload, and the mkosi/APK mini-initrd into `out/fame/fame-linux-fastboot.img`. Latest rebuilt image is packaged with `panic=5`, DTB at `0x88400000`, and ramdisk at `0x88600000`; persistent raw APPSBL U-Boot is back in `UEFI`, so this is again the default non-flashing Linux test path. | Keep using non-flashing `fastboot boot`; refresh artifact hashes after each rebuild. |
| U-Boot | Working | Persistent raw APPSBL U-Boot in `UEFI` cold-boots USB fastboot again after the current MSM USB PHY/reset init changes. Live read-only checks report `U-Boot 2026.07-rc2-00038-ga842285fe134-dirty`, `product: nokia-fame`, `max-fetch-size: 0x04000000`, and a readable `UEFI` partition. | Split and commit the raw APPSBL cold USB fix, then retest Linux `fastboot boot` from the persistent U-Boot path. |
| USB/display debug | Working | UART RX/TX works. Persistent raw APPSBL U-Boot, LK fastboot, and LK-chain U-Boot ChipIdea/ULPI fastboot all enumerate. Linux exposes `ci_hdrc.0` under `/sys/class/udc`, the mini-initrd binds CDC-ACM, and the host sees `usb-Nokia_Lumia_520_CDC_ACM_fame-if00`. | Clean up the remaining USB regulator warnings and turn the U-Boot cold-init fix into a focused patch. |
| Display | MDP4 up to userspace via PLL8; panel WIP | Kernel MDP4 fully up (boot-23): `VERSION=0x4030705`/`v4.3`, `[drm] msm 1.13.0`, `fb0`, reaches userspace. MDP core clock = **PLL8 @128 MHz** (`max_clk=128000000`, commit `3e69030c`); the boot-21 `errors 0x100` + `vblank time out` were a too-slow-core-rate artifact (27 MHz PXO) and are GONE at 128 MHz. Power: a real GFS cycle delivers MDP power (`0x190`; `0x31f`/`0x11f` are valid powered states). NB the boot-22 "PLL2 won't lock" was a MISREAD of status bit16 -- golden state (display-lit EFIESP u-boot) showed firmware drives MDP from PLL2 with bit16=0 too; PLL8 works + is simpler so it's the settled choice, and PLL2's kernel "missing config" is moot (SBL programs L/M/N in HW). Full detail in `notes/display.md`. | (1) **Teisko DSI panel bring-up** -- nothing on the physical display yet: panel reset GPIO 58, DSI PLL, DSI video mode, panel init seq. (2) Consolidate HACK stack: move GFS power-up into `mmcc_msm8960_mdp_pd_power_on()`, retire `mdp4_hack_dump_mmcc()` + diagnostic commits. (3) PLL2 only if gfx3d/csi wanted later (settle-timing fix, not config). |
| Touch | Hypothesis | Android4Lumia says Synaptics; stock ACPI corroborates I2C address `0x4B` plus GPIO11/GPIO52 shape, but the minimal first-boot DTS does not model touch. | Validate controller identity, IRQ flags, reset behavior, and supplies before enabling. |
| Storage | Working | U-Boot initializes SDCC1 as `mmc 0`, reports 8-bit MMC 4.5 `008G92` at 7.3 GiB, reads LBA0, and lists the current live GPT. Linux also enumerates the internal eMMC through SDCC1 with TLMM pinctrl and exposes user, boot0, boot1, and RPMB block devices. | Compare live GPT details with the FFU stock GPT and fix SDCC3/external-SD modeling separately. |
